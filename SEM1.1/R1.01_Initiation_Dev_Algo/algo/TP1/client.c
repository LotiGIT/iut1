/**
* @brief Recette de far breton pour 8 personnes
*/
#include <stdio.h>
#include <stdlib.h>

int main {
    int ht;//ht c'est hors taxe
    int tt;// taux de taxe en pourcent
    //saisie de données
    printf("Quelle est le prix hors taxe ? :");
    scanf("%d" &ht);
    printf("Quelle est la taxe (en pourcent) ? :");
    scanf("%d" &tt);
    printf("Le prix est de %d")



}